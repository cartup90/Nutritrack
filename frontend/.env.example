# URL base de la API del backend.
# En desarrollo, Vite hace proxy de /api → http://localhost:5000 (ver vite.config.js),
# así que el valor por defecto funciona sin configurar nada.
#
# En producción, apunta a tu backend desplegado:
# VITE_API_URL=https://api.tudominio.com/api
VITE_API_URL=/api
